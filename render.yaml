services:
  - type: web
    name: fleet-maintenance-demo
    env: python
    rootDir: fleet_maintenance
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DEMO_MODE
        value: "true"
    plan: free
