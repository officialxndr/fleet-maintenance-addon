services:
  - type: web
    name: fleet-maintenance-demo
    env: python
    buildCommand: pip install -r fleet_maintenance/requirements.txt
    startCommand: cd fleet_maintenance && python app.py
    envVars:
      - key: DEMO_MODE
        value: "true"
    plan: free
